<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme: matches web app dark obsidian palette -->
    <style name="Theme.MarketWatch" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorAccent">@color/system_blue</item>
        <!-- Edge-to-edge: let web content draw behind bars -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
